<!DOCTYPE html>
<html>
  <head>
    <title>Untitled3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <br>
    <div>
      <% if user_signed_in? %>
        <%= link_to "회원정보", edit_user_registration_path %>
        <%= link_to "로그아웃", destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to "로그인", user_session_path %>
        <%= link_to "회원가입", "/users/sign_up" %>
      <% end %>
    </div>
    <hr/>
    <%= alert %>
    <%= notice %>
    <h2 >로그인 정보</h2>

    <% if user_signed_in? %>
      <div>
        아이디 : <%= current_user.email %><br/>
        유저 No. : <%= current_user.id %><br/>
        <%= "팔로워: #{current_user.followers.size} 팔로잉: #{current_user.followings.size}" %>
      </div>

    <% else %>
      로그인 되어있지 않음.
    <% end %>
    <h3><%= link_to "홈", root_path %></h3>
    <hr/>

    <%= yield %>
  </body>
</html>
